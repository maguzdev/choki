# Estado del proyecto

- **Fase actual:** Fase 7 — Billetera, ahorro y metas (completada; QA manual confirmado).
- **Fases completadas:** Fase 0 — Andamiaje; Fase 1 — Base de datos y tipos; Fase 2 — Autenticación, perfiles y guards; Fase 3 — Capa de dominio (TypeScript puro) + tests; Fase 4 — Catálogo: categorías y productos; Fase 5 — Compras e inventario; Fase 6 — POS y registro de venta; Fase 7 — Billetera, ahorro y metas.
- **Funcionalidades implementadas:** `/dinero` con disponible, ahorro, dinero en metas, total actual, ganancia histórica propia/familiar y extracto cronológico filtrable por mes; cada día muestra su cantidad, puede plegarse y presenta inicialmente solo sus cinco movimientos más recientes; configuración de ahorro automático solo para ganancias futuras; movimientos entre disponible y ahorro y registro de dinero utilizado; `/metas` con creación, edición, prioridad, meta principal, pausa, reanudación, cumplimiento y archivo; aportes desde disponible o ahorro; devolución o gasto del dinero de una meta; autocompletado al alcanzar el objetivo, avisos de cercanía/cumplimiento y celebración configurable; `/admin/perfiles` con billeteras y metas infantiles en modo de solo lectura.
- **Validaciones realizadas:** 51/51 tests Vitest ✅; `npx tsc --noEmit` ✅; `npm run lint` ✅; `npm run build` ✅, incluidas `/dinero`, `/metas` y `/admin/perfiles`; `git diff --check` ✅; `supabase db lint --local` ✅; migraciones locales 0001–0006 sincronizadas ✅; prueba transaccional local de aporte, autocompletado, devolución y rechazo de sobregiro, conservando los saldos iniciales y sin efectos parciales ✅; revisión visual a 390 × 844 px, sin desbordamiento horizontal ni errores de consola, con campos visibles calculados a 16 px, switch accesible operativo y extracto mensual plegable ✅; QA manual confirmado por el usuario ✅.
- **Decisiones o desviaciones:** se añadió `wallet_commit` para serializar y confirmar en una sola transacción los movimientos, cambios de meta y notificaciones, revalidando los saldos reales contra retiros concurrentes. Se corrigió el tipo de movimiento de aporte a meta a `GOAL_IN`, coherente con el esquema. El interruptor artesanal de ahorro fue reemplazado por `Switch` de Base UI y los controles de formulario mantienen al menos 16 px en móvil para evitar el zoom automático de iOS sin restringir el zoom manual. La semilla añade metas de ejemplo solo cuando no existe una del mismo nombre; no se ejecutó sobre los datos actuales. `PLAN.md` conserva como regla autorizada que ninguna venta puede superar el stock.
- **Pendientes conocidos:** regularizar el dato heredado `Barquillo Chococream` con stock `-5` mediante una compra suficiente o una corrección trazable; las ventas nuevas ya no pueden generar stock negativo.
- **Siguiente fase:** Fase 8 — Gamificación: progreso, racha y premios (no iniciada).
