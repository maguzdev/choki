# Estado del proyecto

- **Fase actual:** Fase 6 — POS y registro de venta (completada; QA manual confirmado).
- **Fases completadas:** Fase 0 — Andamiaje; Fase 1 — Base de datos y tipos; Fase 2 — Autenticación, perfiles y guards; Fase 3 — Capa de dominio (TypeScript puro) + tests; Fase 4 — Catálogo: categorías y productos; Fase 5 — Compras e inventario; Fase 6 — POS y registro de venta.
- **Funcionalidades implementadas:** POS Mobile First para niño y padre; catálogo por categorías con carrito persistente en `sessionStorage`; límite visible que impide agregar más unidades que el stock y recorta carritos restaurados al máximo vigente; cobro en efectivo o transferencia; opciones rápidas, teclado de efectivo, cálculo de cambio y propina total o parcial; confirmación idempotente con identificador compatible con navegadores móviles bajo HTTP local; validación de stock en servidor y descuento condicional atómico para impedir ventas concurrentes con inventario insuficiente; registro de venta, detalle, inventario, ganancias y movimientos; ahorro automático; reparto familiar del padre; XP, puntos, niveles, logros, retos, rachas, protectores, notificaciones y alertas de stock; resumen final diferenciado por tipo de vendedor y celebraciones configurables.
- **Validaciones realizadas:** 49/49 tests Vitest ✅, incluido rechazo de payload con stock insuficiente; `npx tsc --noEmit` ✅; `npm run lint` ✅; `npm run build` ✅, incluidas `/vender` y `/admin/vender`; `git diff --check` ✅; `supabase db lint --local` ✅; migración incremental `0005_prevent_negative_sale_stock.sql` aplicada sin reset ✅; prueba directa de concurrencia/atomicidad: intento de descontar 21 unidades con stock 20 rechazado como `INSUFFICIENT_STOCK`, conservando stock 20 y sin efectos parciales ✅; QA técnico y manual en dispositivo móvil del carrito, efectivo, cambio, propina, transferencia y compatibilidad bajo HTTP local ✅; integración local con ventas infantil y de padre, verificando inventario, ahorro, reparto exacto, XP, puntos, logro, reto y racha ✅; cierre de QA confirmado por el usuario ✅.
- **Decisiones o desviaciones:** regla de stock modificada con autorización del usuario y documentada en `PLAN.md`: ninguna venta puede superar las existencias. La protección vive en UI, dominio, Server Action y RPC atómica; el movimiento de inventario toma el `stock_after` real de la transacción. El servidor vuelve a consultar precios, costos, stock, reglas y progresión antes de confirmar; el cliente nunca determina los importes persistidos. Cada apertura del cobro crea un identificador de venta que se conserva durante reintentos para impedir duplicados; si `crypto.randomUUID()` no está disponible por usar una IP local sin HTTPS, se genera el UUID v4 con `crypto.getRandomValues()`. La semilla incorpora la configuración mínima de juego requerida por esta fase. Se conservan tres ventas técnicas en la base local para no borrar los datos realistas creados por el usuario; el QA debe comparar estados antes/después, no asumir contadores en cero.
- **Pendientes conocidos:** regularizar el dato heredado `Barquillo Chococream` con stock `-5` mediante una compra suficiente o una corrección trazable; el POS ya lo bloquea y ninguna venta nueva puede generar stock negativo.
- **Siguiente fase:** Fase 7 — Billetera, ahorro y metas (no iniciada).
