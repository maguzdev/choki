# Estado del proyecto

- **Fase actual:** Fase 4 — Catálogo: categorías y productos (completada; QA manual confirmado).
- **Fases completadas:** Fase 0 — Andamiaje; Fase 1 — Base de datos y tipos; Fase 2 — Autenticación, perfiles y guards; Fase 3 — Capa de dominio (TypeScript puro) + tests; Fase 4 — Catálogo: categorías y productos.
- **Funcionalidades implementadas:** motor puro de fechas locales, dinero y reparto por mayor resto; costo promedio de compras; totales, costos, margen, cambio, propina y opciones rápidas de venta; atribución de ganancias propias y familiares; ahorro automático y constructores de movimientos de billetera; XP, puntos, niveles, logros, retos y rachas; payloads completos de ventas; CRUD protegido de categorías y productos, selector fijo de 40 emojis sin duplicados, sincronización correcta al editar, cierre del modo edición al guardar, diálogos propios de eliminación y vista infantil de inventario desde `products_public` sin costos.
- **Validaciones realizadas:** 47/47 casos obligatorios de §J en verde con Vitest ✅; `npx tsc --noEmit` ✅; `npm run lint` ✅; `npm run build` ✅; nuevas rutas `/admin/productos` y `/inventario` incluidas en el build ✅; la ruta infantil sin sesión redirige a `/login` ✅; `git diff --check` ✅.
- **Decisiones o desviaciones:** ninguna respecto al alcance. El catálogo se lee con el cliente de sesión; las mutaciones se autorizan con `requireParent()` y usan `service role`. La consulta infantil selecciona explícitamente solo las columnas de `products_public`, sin `cost` ni `avg_cost`.
- **Pendientes conocidos:** ninguno dentro de la Fase 4. La comprobación de borrado de un producto con ventas se realizará cuando exista el flujo de ventas de la Fase 6; la protección ya está implementada.
- **Siguiente fase:** Fase 5 — Compras e inventario (no iniciada).
