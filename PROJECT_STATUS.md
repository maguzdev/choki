# Estado del proyecto

- **Fase actual:** Fase 5 — Compras e inventario (completada; QA manual confirmado).
- **Fases completadas:** Fase 0 — Andamiaje; Fase 1 — Base de datos y tipos; Fase 2 — Autenticación, perfiles y guards; Fase 3 — Capa de dominio (TypeScript puro) + tests; Fase 4 — Catálogo: categorías y productos; Fase 5 — Compras e inventario.
- **Funcionalidades implementadas:** motor puro de fechas locales, dinero y reparto por mayor resto; costo promedio de compras; totales, costos, margen, cambio, propina y opciones rápidas de venta; atribución de ganancias propias y familiares; ahorro automático y constructores de movimientos de billetera; XP, puntos, niveles, logros, retos y rachas; payloads completos de ventas; CRUD protegido de categorías y productos, selector fijo de 40 emojis sin duplicados, sincronización correcta al editar, cierre del modo edición al guardar, diálogos propios de eliminación y vista infantil de inventario desde `products_public` sin costos; registro de compras con previsualización del unitario/promedio/stock, historial de compras con promedio resultante, ajustes manuales trazados por motivo y nota, e inventario administrativo con alertas de bajo stock e historial por producto.
- **Validaciones realizadas:** 47/47 casos obligatorios de §J en verde con Vitest ✅; `npx tsc --noEmit` ✅; `npm run lint` ✅; `npm run build` ✅; rutas `/admin/compras` y `/admin/inventario` incluidas en el build ✅; `git diff --check` ✅.
- **Decisiones o desviaciones:** ninguna respecto al alcance. Las compras usan `purchase_commit` con filas completas y la misma función `applyPurchase` que alimenta la previsualización. Los ajustes manuales no permiten reducir el stock por debajo de cero y, si el movimiento falla, restauran el stock. El `cost` manual se usa como costo inicial/de respaldo mientras `avg_cost` es cero; tras compras, las ventas usan el promedio ponderado. Las compras permanecen inmutables: por ahora no se editan ni anulan para no alterar inventario o costos históricos. Los formularios se reinician al guardar para evitar reutilizar estados de éxito o datos de la operación anterior.
- **Pendientes conocidos:** ninguno en la Fase 5.
- **Siguiente fase:** Fase 6 — POS y registro de venta (no iniciada).
