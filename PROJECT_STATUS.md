# Estado del proyecto

- **Fase actual:** Fase 2 — Autenticación, perfiles y guards (completada; ejecución detenida).
- **Fases completadas:** Fase 0 — Andamiaje; Fase 1 — Base de datos y tipos; Fase 2 — Autenticación, perfiles y guards.
- **Funcionalidades implementadas:** clientes Supabase de sesión y `service_role` solo servidor; refresh y redirección por rol en middleware; sesión cacheada por request; guards de niño, padre y niño propio; contraseña infantil derivada por HMAC; login de padres y niños; bloqueo de PIN por 5 intentos durante 5 minutos; protección contra doble autoenvío del PIN; logout; acciones para establecer/cambiar PIN; selector Mobile First de perfiles y teclado PIN; layouts protegidos; semilla idempotente de 2 padres y 2 niños con ajustes de ahorro, reparto y racha inicial.
- **Validaciones realizadas:** semilla ejecutada y repetida sin duplicados ✅; 4 usuarios Auth/4 perfiles/2 configuraciones infantiles/2 repartos/2 rachas ✅; los 4 perfiles autentican con sus credenciales semilla ✅; login real de padre y niño, redirecciones cruzadas y logout ✅; quinto PIN erróneo activa bloqueo visible y la semilla posterior restauró el estado ✅; un PIN inválido desde el teclado ahora registra exactamente un fallo y muestra 4 intentos restantes ✅; revisión visual a 390 px ✅; `npm run lint` ✅; `npm test` ✅ (0 pruebas, esperado hasta la Fase 3); `npx tsc --noEmit` ✅; `npm run build` ✅; secretos ausentes de `.next/static` ✅; `git diff --check` ✅.
- **Decisiones o desviaciones:** `.env.local` se creó con las claves de la instancia local y permanece ignorado por Git. `scripts/seed.ts` contiene solo la porción de usuarios exigida por esta fase; catálogo y demás datos se añadirán en sus fases correspondientes. Las advertencias de `origin` observadas durante la prueba provinieron del cliente HTTP de validación, no de la aplicación en navegador.
- **Pendientes conocidos:** Ninguno dentro de la Fase 2. Las pantallas de inicio son checkpoints deliberados; dashboards y navegación funcional pertenecen a fases posteriores.
- **Siguiente fase:** Fase 3 — Capa de dominio (TypeScript puro) + tests.
