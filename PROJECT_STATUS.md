# Estado del proyecto

- **Fase actual:** Fase 8 — Gamificación: progreso, racha y premios (completada; QA manual confirmado).
- **Fases completadas:** Fase 0 — Andamiaje; Fase 1 — Base de datos y tipos; Fase 2 — Autenticación, perfiles y guards; Fase 3 — Capa de dominio (TypeScript puro) + tests; Fase 4 — Catálogo: categorías y productos; Fase 5 — Compras e inventario; Fase 6 — POS y registro de venta; Fase 7 — Billetera, ahorro y metas; Fase 8 — Gamificación: progreso, racha y premios.
- **Funcionalidades implementadas:** `/progreso` con nivel, barra, niveles, logros, retos, ranking e historial mensual agrupado por día; `/racha` con replay, calendario y tres protectores gratuitos iniciales que pueden reponerse después de consumirse, sin superar nunca la capacidad de tres; `/premios` con catálogo, canje e historial mensual; `/admin/gamificacion` y `/admin/recompensas` configurables. Todos los historiales actuales (dinero, progreso, canjes, compras e inventario) comparten filtro mensual, grupos diarios plegables y cinco movimientos iniciales por día. Los controles de configuración activos/visibles usan el switch compartido de ahorro automático. Crear o editar un premio limpia/cierra el formulario correctamente, y el stock vacío se conserva como ilimitado. El resumen de venta identifica el nivel actual y cuánto falta para el siguiente.
- **Validaciones realizadas:** 57/57 tests Vitest ✅, incluidos concesión inicial y reposición de protectores, tope máximo de tres y stock ilimitado frente a stock cero; `npx tsc --noEmit` ✅; `npm run lint` ✅; `npm run build` ✅; `supabase db lint --local` ✅; migraciones locales 0001–0008 y tipos sincronizados ✅; estado local verificado con tres protectores y un único evento `GRANT` inicial por niño ✅; edición real de recompensa comprobada en navegador, con persistencia del cambio, cierre del editor, switches accesibles y consola sin advertencias ni errores ✅; QA manual de la Fase 8 confirmado por el usuario ✅.
- **Decisiones o desviaciones:** Se corrigió y documentó la regla de negocio: cada niño recibe tres protectores gratuitos al crearse, una falla consume uno y los gastados pueden reponerse mediante canje hasta volver a tres; `protector_max` nunca puede superar tres. La entrega inicial queda registrada como evento `GRANT` trazable y la migración 0008 regulariza los perfiles existentes sin duplicar la concesión. `reward_redeem`, `redemption_update` y `streak_refresh` mantienen sus efectos en transacciones consistentes. Un logro desbloqueado no puede borrarse y un reto con progreso no puede eliminarse. La semilla completa no se reejecutó para preservar los datos actuales.
- **Pendientes conocidos:** Regularizar el dato heredado `Barquillo Chococream` con stock `-5` mediante una compra suficiente o una corrección trazable.
- **Siguiente fase:** Fase 9 — Dashboards y estadísticas (no iniciada).
